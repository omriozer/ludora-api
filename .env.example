# Ludora API Environment Configuration Template
# =============================================
#
# Copy this file to create environment-specific configurations:
#   .env.development  - Local development
#   .env.staging      - Staging/testing environment
#   .env.production   - Production environment (KEEP SECURE!)
#
# IMPORTANT: Never commit actual credentials to version control!
#
# The env-validator script uses this file as a template to determine
# which variables are required for deployment. Variables marked as
# [OPTIONAL] will not block deployment if missing.

# =============================================================================
# CORE CONFIGURATION
# =============================================================================

# Environment identifier (development | staging | production)
ENVIRONMENT=production

# Server port (Heroku sets this automatically in production)
PORT=3003

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Full database connection URL (preferred for Heroku)
# Format: postgres://user:password@host:port/database
DATABASE_URL=postgres://user:password@host:5432/ludora_db

# Individual database settings (fallback if DATABASE_URL not used)
DB_HOST=your-db-host
DB_PORT=5432
DB_NAME=ludora_db
DB_USER=ludora_user
DB_PASSWORD=your-secure-password

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================

# JWT secret for token signing
# Generate with: openssl rand -base64 64
JWT_SECRET=your-jwt-secret-at-least-64-characters-long

# JWT token expiration (default: 24h)
JWT_EXPIRES_IN=24h

# API key for internal service authentication
# Generate with: openssl rand -hex 32
API_KEY=your-api-key

# Encryption key for sensitive data
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your-encryption-key

# Admin password for maintenance mode access
ADMIN_PASSWORD=your-admin-password

# =============================================================================
# FIREBASE CONFIGURATION
# =============================================================================

# Base64 encoded Firebase service account JSON
# Get from: Firebase Console > Project Settings > Service Accounts > Generate new private key
# Encode with: cat service-account.json | base64
FIREBASE_SERVICE_ACCOUNT=base64-encoded-service-account-json

# =============================================================================
# FILE STORAGE (AWS S3)
# =============================================================================

# Enable S3 storage (true for staging/production)
USE_S3=true

# Local storage path (used when USE_S3=false)
LOCAL_STORAGE_PATH=./uploads

# S3 bucket name (environment-specific)
# Development: ludora-files-dev
# Staging: ludora-files-staging
# Production: ludora-files-prod
AWS_S3_BUCKET=ludora-files-prod

# AWS credentials
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=eu-central-1

# =============================================================================
# PAYMENT CONFIGURATION (PAYPLUS)
# =============================================================================

# PayPlus Payment Gateway Credentials
# Get from: https://www.payplus.co.il/ dashboard
# Each environment should use appropriate values (production/staging)
PAYPLUS_API_KEY=your-payplus-api-key
PAYPLUS_SECRET_KEY=your-payplus-secret-key
PAYPLUS_PAYMENT_PAGE_UID=your-payplus-payment-page-uid
PAYPLUS_TERMINAL_UID=your-payplus-terminal-uid

# =============================================================================
# EXTERNAL AI SERVICES [OPTIONAL]
# =============================================================================

# OpenAI API key for AI features
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic API key for Claude features
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Default LLM model to use
DEFAULT_LLM_MODEL=gpt-3.5-turbo

# =============================================================================
# EMAIL CONFIGURATION [OPTIONAL]
# =============================================================================

# SMTP server settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=noreply@ludora.app

# =============================================================================
# FRONTEND & CORS CONFIGURATION
# =============================================================================

# Primary frontend URL
FRONTEND_URL=https://ludora.app

# API URL (public-facing)
API_URL=https://api.ludora.app

# Additional allowed frontend URLs (comma-separated)
# Used for multiple domains, Firebase previews, etc.
ADDITIONAL_FRONTEND_URLS=https://ludora.app,https://www.ludora.app,https://ludora-af706.web.app

# Webhook allowed origins (comma-separated)
# Leave empty to use default CORS settings
WEBHOOK_ALLOWED_ORIGINS=

# API documentation URL [OPTIONAL]
API_DOCS_URL=https://api.ludora.app/docs

# =============================================================================
# NOTIFICATIONS [OPTIONAL]
# =============================================================================

# Telegram bot for deployment notifications
# Get from: https://core.telegram.org/bots#creating-a-new-bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id

# =============================================================================
# DEBUGGING & DEVELOPMENT [OPTIONAL]
# =============================================================================

# Enable debug mode for specific user (email address or false)
DEBUG_USER=false

# Override CORS for development (set to false in production!)
CORS_DEV_OVERRIDE=false

# Maximum request body size in bytes (default: 50MB)
MAX_REQUEST_SIZE=52428800
